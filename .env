# Copy this file to .env for local runs (never commit the real .env file).
# For GitHub Actions, set these as repository secrets instead
# (Settings -> Secrets and variables -> Actions -> New repository secret).

# Get these from @BotFather on Telegram (bot token) and
# https://api.telegram.org/bot<TOKEN>/getUpdates (chat id)
TELEGRAM_BOT_TOKEN="8870274598:AAG1u9PX2-21nPAUuiyC_MHCiaHR5EUJFQw"
TELEGRAM_CHAT_ID="1082143881"

# Optional overrides
DB_PATH=signals.db
CSV_EXPORT_PATH=signals_export.csv
